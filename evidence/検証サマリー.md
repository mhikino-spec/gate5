# 検証サマリー

検証日：2026-09-12（JST）

- Python：3.12.14 / SQLite：3.53.1 / Windows。
- 実行：python -m unittest discover -s tests -v
- 結果：27 tests / OK。詳細はtest_results.txt。
- 同時端末・同時借用者・同一トークンの競合を実行し、成立件数を検証。
- 貸出INSERT後、返却UPDATE後、監査後の例外注入でロールバックを検証。
- 不正CSRF（日本語を含む）、直接GET、確認後差替え、未認証、HTMLエスケープをHTTP経由で検証。
- 実ブラウザ：正常完了、E-02、E-11、E-07、E-08を表示・目視確認しPNG保存。
- 画面撮影後のDB確認：PC-0001の未返却先1002が1件、PC-0005の未返却先1004が1件、PC-0006の貸出0件。競合の敗者1003と上限超過の要求は登録されていない。
- 図：貸出7・返却5の判断を含むPNGを目視確認。
- 検証用サーバー・タブは終了。起動はREADME記載のコマンドで再現可能。

テストは一時DB、撮影は専用preview.sqlite3で実行。ZIPへデモDB、パスワードハッシュ、セッション情報を含めない。正常な起動時のseed処理が架空データを作る。
