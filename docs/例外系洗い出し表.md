# 例外系洗い出し表

36項目・8観点。前半4列は要求・補完案、後半3列は今回の実装状況です。「実装済」は本提出版の範囲内を意味します。業務上の正式合意や全運用機能の完成を意味しません。E-16・E-21は要求文書の将来設計用で、現実装の表示コードではありません。

|No・観点|状況・起きる問題|検知時点・決めた仕様（要求）|画面表示・優先度|実装状況|検証・根拠|制約・未実装理由|
|---|---|---|---|---|---|---|
|X-01 在庫・状態|端末ID不存在。無効な貸出が残る|ID入力・送信・確定時に存在確認。更新なし|E-01 中|実装済|test_device_states|app.pyで検知・拒否／正常復帰を実装。|
|X-02 在庫・状態|端末がLENT。二重貸出になる|確定時に状態再確認。要求を拒否|E-02 高|実装済|test_device_states／mock02|app.pyで検知・拒否／正常復帰を実装。|
|X-03 在庫・状態|REPAIR・DISPOSEDを借りる|表示時は選択不可、確定時も拒否|E-03 中|実装済|test_device_states|app.pyで検知・拒否／正常復帰を実装。|
|X-04 在庫・状態|貸出可能0件。空の選択肢で進める|画面取得成功後に件数確認。入力前に案内|E-04 低|実装済|test_last_device／loan_page|app.pyで検知・拒否／正常復帰を実装。|
|X-05 在庫・状態|TABLET・MONITORの値を送信|確認・確定時に種別確認。拒否|E-05 中|実装済|test_device_states|app.pyで検知・拒否／正常復帰を実装。|
|X-06 人の状態|休職・退職者が申し込む|確認・確定時に在籍状態確認|E-07 中|実装済|test_employee_states／mock04|app.pyで検知・拒否／正常復帰を実装。|
|X-07 人の状態|社員レコードなし。借用者不明になる|借用者指定・確定時に存在確認|E-06 中|実装済|test_employee_states|app.pyで検知・拒否／正常復帰を実装。|
|X-08 人の状態|既に1台未返却。上限を超える|確定時に借用者の未返却件数確認|E-08 高|実装済|test_employee_states／mock05|app.pyで検知・拒否／正常復帰を実装。|
|X-09 人の状態|許可範囲外の代理操作|確定前に操作主体と借用者の権限確認|E-18 高|一部実装|test_unauthorized_return_and_actor|代理貸出は全ACTIVE社員に許可するデモ方針。正式な許可範囲はQ-05未決。返却は本人／管理者のみ。|
|X-10 重複・多重|同じ端末へ2人が同時申請|確定処理で端末確保を直列化。成功は最大1件|E-02／最後の1台はE-04 高|実装済|test_concurrent_same_device／test_last_device／mock02|app.pyで検知・拒否／正常復帰を実装。|
|X-11 重複・多重|同じ社員が異なる端末を同時申請|借用者単位で上限判定と登録を一体化|E-08 高|実装済|test_concurrent_same_borrower|app.pyで検知・拒否／正常復帰を実装。|
|X-12 重複・多重|確定ボタン連打・二重送信|要求識別子で重複検知。同じ結果を返す|既存完了表示 高|実装済|test_idempotent_concurrent_and_restart|app.pyで検知・拒否／正常復帰を実装。|
|X-13 重複・多重|完了後に戻って再確定|使用済み要求の結果を照会し再登録しない|既存完了表示 高|実装済|test_http_normal_replay_html_escape|app.pyで検知・拒否／正常復帰を実装。|
|X-14 入力値|必須値の未入力・空白のみ|確認遷移前と確定時に検証|E-09～E-12 中|実装済|test_ids／test_purpose／test_dates_and_boundaries|app.pyで検知・拒否／正常復帰を実装。|
|X-15 入力値|目的が101文字以上。DB切捨て・障害|正規化後の文字数を検証し拒否|E-12 中|実装済|test_purpose|app.pyで検知・拒否／正常復帰を実装。|
|X-16 入力値|2026/02/30等の存在しない日付|日付解析時に拒否。自動補正しない|E-10 中|実装済|test_dates_and_boundaries|app.pyで検知・拒否／正常復帰を実装。|
|X-17 入力値|返却予定日が過去日・上限超過|確定時の業務日を基準に範囲確認|E-11 中|実装済|test_dates_and_boundaries／mock03|app.pyで検知・拒否／正常復帰を実装。|
|X-18 入力値|当日・上限ちょうどで判定が不一致|合意した両端の包含条件を画面・サーバーで統一|範囲内は確認へ 中|実装済|test_dates_and_boundaries|app.pyで検知・拒否／正常復帰を実装。|
|X-19 入力値|絵文字・HTML・制御文字で文字化けや実行|文字仕様検証と表示エスケープ。制御文字拒否|通常の文字表示／E-12 高|実装済|test_purpose／test_http_normal_replay_html_escape|app.pyで検知・拒否／正常復帰を実装。|
|X-20 入力値|選択肢外・桁あふれIDを直接送信|サーバーで型・範囲・存在・種別を確認|E-09／E-01／E-05 中|実装済|test_ids／test_device_states|app.pyで検知・拒否／正常復帰を実装。|
|X-21 順序|借りていない端末を返す|貸出IDで未返却レコードを確認し更新なし|E-14 高|実装済|test_return_duplicates_and_old_loan|app.pyで検知・拒否／正常復帰を実装。|
|X-22 順序|返却済みを再返却。履歴が変わる|既返却を検知。同じ要求は既存結果、別要求は拒否|完了結果／E-14 高|実装済|test_return_duplicates_and_old_loan|app.pyで検知・拒否／正常復帰を実装。|
|X-23 順序|貸出中に修理・廃棄へ変更される|状態変更側で制御する案。返却時は不整合を検知して中止|E-20 高|一部実装|test_database_guards／test_inconsistent_device_rejected|貸出中の状態変更をDBトリガーで拒否。修理・廃棄管理画面や例外運用は未実装。|
|X-24 順序|古い貸出の返却要求が再貸出後に届く|貸出IDと未返却条件で限定。新貸出を更新しない|既存結果／E-14 高|実装済|test_return_duplicates_and_old_loan|app.pyで検知・拒否／正常復帰を実装。|
|X-25 時間|期限超過が放置される|日次等の判定処理で未返却かつ期限超過を抽出|借用者へ延滞メール 中|一部実装|returns_page（表示のみ）|返却一覧の延滞表示は実装。日次処理・メール送信は宛先と頻度未決のため未実装。|
|X-26 時間|延滞者が新規に借りる|確定時に延滞を検知。返却は許容|E-13 中|実装済|test_employee_states／test_overdue_can_return|app.pyで検知・拒否／正常復帰を実装。|
|X-27 時間|確認中に日付が変わる|確定時の業務日で再検証し必要なら入力へ戻す|E-11／E-21 中|実装済|test_midnight_revalidation|app.pyで検知・拒否／正常復帰を実装。|
|X-28 時間|月末・1か月先・1週間の境界が曖昧|暦月・日数のルールをQ-02・Q-03で統一|範囲内は確認／範囲外はE-11・E-21 中|一部実装|test_dates_and_boundaries（実貸出のみ）|実貸出のD〜D+7は実装。将来予約の暦月境界は予約設計が未確定のため未実装。|
|X-29 失敗時|2テーブルの片方更新後に障害|貸出・返却を各1トランザクションにして全体取消|E-15 高|実装済|test_loan_rollback_and_retry／test_return_rollback|app.pyで検知・拒否／正常復帰を実装。|
|X-30 失敗時|DB切断・タイムアウト|未確定が判明した場合のみ失敗通知。確定不明は結果照会|E-15／E-16 高|一部実装|test_loan_rollback_and_retry|DB例外はロールバックしてE-15。同じ要求の再送は安全。通信結果不明専用のE-16画面・自動照会UIは未実装。|
|X-31 失敗時|確定後に応答通信断・セッション失効|再認証後も元の要求結果を取得し再登録しない|E-16／E-19 高|一部実装|test_idempotent_concurrent_and_restart／test_unauthenticated_no_update|要求結果はDBに保存し、同じ要求を再送・元の結果URLを再訪できる。自動再接続・結果一覧UIは未実装。|
|X-32 失敗時|延滞メール送信失敗|送信結果を記録し再試行。延滞判定は継続|運用向け送信失敗記録 中|未実装|未実装|メール基盤・宛先・頻度が未決。送信失敗ログ／再試行キューは今回対象外。|
|X-33 不正操作|URL直打ちで確認を省略|確定時に有効な確認情報を必須化。GETでは更新なし|E-17 高|実装済|test_get_cannot_mutate／test_tokens_ownership_and_expiry|app.pyで検知・拒否／正常復帰を実装。|
|X-34 不正操作|確認後に端末・借用者・日付を差替え|確認済み内容と照合。不一致は再確認へ|E-17 高|実装済|test_tampered_confirmation|app.pyで検知・拒否／正常復帰を実装。|
|X-35 不正操作|他人のIDを指定し権限外操作|本人または許可代理かをサーバーで判定|E-18 高|一部実装|test_unauthorized_return_and_actor／test_tokens_ownership_and_expiry|借用者IDによる代理貸出はデモで許可。返却・要求トークンは本人／管理者・操作者で制限。正式な代理範囲は未合意。|
|X-36 不正操作|未認証・期限切れ状態から実行|更新前に認証確認。完了済みなら再ログイン後照会|E-19 高|実装済|test_unauthenticated_no_update／test_tokens_ownership_and_expiry|app.pyで検知・拒否／正常復帰を実装。|

## 観点別件数

|観点|件数|
|---|---:|
|在庫・状態|5|
|人の状態|4|
|重複・多重|4|
|入力値|7|
|順序|4|
|時間|4|
|失敗時|4|
|不正操作|4|
|合計|36|

実装済28件・一部実装7件・未実装1件。メール関連X-25も一部実装として扱い、送信まで完成したとは数えていません。
